# Web_Security_Analyzer_v3 example environment variables
SECRET_KEY=replace_with_secure_value
JWT_SECRET_KEY=replace_with_secure_value
DATABASE_PATH=users.db
RATELIMIT_STORAGE_URI=memory://
PORT=5000
FLASK_DEBUG=False
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
ABUSEIPDB_API_KEY=
CREATE_DEFAULT_ADMIN=False
DEFAULT_ADMIN_PASSWORD=
# Web Security Analyzer v3.0 - Environment Configuration
# Copy this file to .env and fill in your values

# Security
SECRET_KEY=your-super-secret-key-here-change-this-in-production
JWT_SECRET_KEY=your-jwt-secret-key-here-change-this-in-production

# Database
DATABASE_PATH=users.db

# Rate Limiting
# For production, use Redis: redis://localhost:6379/0
RATELIMIT_STORAGE_URI=memory://

# Email (SMTP)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@websecurity.app

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:5000,http://127.0.0.1:5000

# SocketIO
SOCKETIO_CORS=*

# External APIs (optional)
ABUSEIPDB_API_KEY=your-abuseipdb-api-key

# Server
PORT=5000
FLASK_DEBUG=False
SESSION_COOKIE_SECURE=False

# Production Settings
# Set SESSION_COOKIE_SECURE=True when using HTTPS
# Set FLASK_DEBUG=False in production
